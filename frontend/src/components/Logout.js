import React from "react";

const Logout = () => {
  return (
    <form>
      <input type="text" />
      <textarea />
      <button type="submit">Test</button>
    </form>
  );
};

export default Logout;
